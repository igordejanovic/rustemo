[package]
name = "bootstrap"
version = "0.1.0"
edition = "2021"
description = "Rustemo bootstrapping CLI helper"

# See more keys and their definitions at https://doc.rust-lang.org/cargo/reference/manifest.html

[dependencies]
clap = { version = "3.2.13", features = ["derive"] }
